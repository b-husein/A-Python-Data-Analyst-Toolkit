
# an extra data type called 'complex'; 
# a complex number is third type; first is int, second is float.
# we use it if we do complex math.

# numbers get stored in memory as binary and there is bin() action that we can use;
# for example for number 5;

print(bin(5)) # output is 0b101

